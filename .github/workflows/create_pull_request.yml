name: Create Pull Request

on:
  repository_dispatch:
    types: [create-pr]


jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    
    - name: Create pull request for ${{ github.event.client_payload.image_version }}
      uses: actions/github-script@v2
      with:
        github-token: ${{secrets.GITHUB_TOKEN}}
        script: |
          octokit.pulls.create({
            ${{ github.event.client_payload.organization }},
            ${{ github.event.client_payload.repository }},
            ${{ github.event.client_payload.pr_title }},
            ${{ github.event.client_payload.pr_branch }},
            ${{ github.event.client_payload.pr_base }},
            ${{ github.event.client_payload.pr_body }},
          });
